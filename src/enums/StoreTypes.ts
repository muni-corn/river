export enum StoreActions {
    HistoryPush = "historyPush",
    Login = "login",
    Logout = "logout",
    Register = "register"
}

export enum StoreMutations {
    HistoryPush = "historyPush",
    SetToken = "setToken",
    ClearToken = "clearToken"
}
